<% @articles.each do |article| %>
  <div class="article_wrapper">
    <h2 class="title"> <%= link_to article.title, article_path(article) %></h2>
    <p class="article_timestamp"> <%= article.created_at.strftime("%B, %d, %Y") %> </p>
  </div>
<% end %>

